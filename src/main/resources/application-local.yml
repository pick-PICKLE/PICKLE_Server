spring:
  session:
    jdbc:
      initialize-schema: always
    store-type: jdbc
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    url: jdbc:h2:file:/Users/yousungwook/h2DbList/sinzak;DB_CLOSE_ON_EXIT=FALSE
  h2:
    console:
      enabled: 'true'
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
